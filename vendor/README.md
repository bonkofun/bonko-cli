# Candidate SDK

Local build of `@bonko/template-sdk` 0.3.0 from the sibling SDK repository.
This archive pins the reviewed implementation before a public release exists.
Do not substitute an unreviewed registry build or interpret this as publication.

SHA-256: `2d2878bba5e9832e565ac7ec5451f094243717b56fcaf027a191fa925f043a8f`

The CLI bundles this dependency so archive installation remains self-contained.
Replace the file dependency with the exact registry version only after coordinated release.
