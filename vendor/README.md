# Local SDK candidate

`bonko-template-sdk-0.3.1.tgz` is the checked local progressive-video SDK candidate,
not a published npm release. It is vendored so this branch builds independently.
Replace the file dependency with the exact public version after an authorized
SDK release; coordinate CLI, Admin, main-site and runtime rollout. Do not publish
a consumer release while this candidate dependency remains.
